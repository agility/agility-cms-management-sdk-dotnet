# Agility CMS Management SDK Test Configuration
# Copy this file to .env and populate with your actual values

# Your Agility CMS instance GUID
Guid=your-instance-guid-here

# Locale for your instance (e.g., en-us)
Locale=en-us

# Personal Access Token (PAT) for authentication
# Generate this from your Agility CMS instance settings
AccessToken=your-personal-access-token-here

# Optional: Azure Table Storage connection string for token caching
# Leave empty if not using token caching
StorageConnectionString=

# Optional: Table name for token storage
# Leave empty if not using token caching
TableName=
